{
  "name": "faceray-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "FaceRay desktop shell (Tauri 2.0) — control plane for the Python CV sidecar.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-shell": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "^5.6.0",
    "vite": "^6"
  }
}
